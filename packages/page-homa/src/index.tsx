import React, { FC } from 'react';
import { Page, PageTitle } from '@honzon-platform/ui-components';

const PageHoma: FC = () => {
  return (
    <Page>
      <PageTitle>Homa</PageTitle>
    </Page>
  );
};

export default PageHoma;
